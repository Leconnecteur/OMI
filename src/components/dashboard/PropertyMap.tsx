import React from 'react';
import { Box } from '@mui/material';

const PropertyMap = () => {
  return (
    <Box>
      {/* Contenu de la carte à implémenter */}
      <div>Carte des ventes à venir</div>
    </Box>
  );
};

export default PropertyMap;
